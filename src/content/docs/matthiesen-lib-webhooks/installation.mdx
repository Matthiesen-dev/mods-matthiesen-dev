---
title: "Installation"
description: "How to add Matthiesen Lib Webhooks as a dependency to your Minecraft mod project"
prev: 
  link: /matthiesen-lib-webhooks/getting-started
  label: "Getting Started"
next: 
  link: /matthiesen-lib-webhooks/examples
  label: "Examples"
sidebar:
  order: 2
banner:
  content: |
    These docs are a work in progress and may be incomplete or contain inaccuracies. (Or be for a newer unreleased version of the mod)
---

import ReadMore from '~/components/ReadMore.astro';
import MavenBadge from '~/components/MavenBadge.astro';

## Adding Matthiesen Lib Webhooks to Your Project

Matthiesen Lib Webhooks is available through the [Matthiesen Dev Maven](https://maven.matthiesen.dev/) repository. Follow the instructions below to add it to your Gradle project.

## Prerequisites

- Minecraft 1.21.1
- Java 21
- Fabric Loader 0.17.2+ (for Fabric)
- NeoForge 21.1.182+ (for NeoForge)

## Gradle Configuration

### Step 1: Add the Repository

Add the Matthiesen Dev Maven repository to your `build.gradle` or `build.gradle.kts`:

```kotlin
repositories {
    maven("https://maven.matthiesen.dev/releases") {
        name = "devMatthiesenMaven-releases"
        content {
            includeGroup("dev.matthiesen")
        }
    }
    // For Snapshot versions, also include the snapshots repository
    maven("https://maven.matthiesen.dev/snapshots") {
        name = "devMatthiesenMaven-snapshots"
        content {
            includeGroup("dev.matthiesen")
        }
    }
}
```

### Step 2: Add Version to gradle.properties

Add the Matthiesen Lib API version to your `gradle.properties` file:

```properties
matthiesen_lib_webhooks_version=1.0.0
```

:::tip[Finding the Latest Version]
Visit [Matthiesen Dev Maven](https://maven.matthiesen.dev/) to find the latest version for your platform.
:::

| Current Release Versions |
| ------- |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-common' settings={{ name: "matthiesen-lib-webhooks-common" }} /> |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-fabric' settings={{ name: "matthiesen-lib-webhooks-fabric" }} /> |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-neoforge' settings={{ name: "matthiesen-lib-webhooks-neoforge" }} /> |

| Current Snapshot Versions |
| ------- |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-common' settings={{ name: "matthiesen-lib-webhooks-common", color: "A844D4" }} snapshot /> |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-fabric' settings={{ name: "matthiesen-lib-webhooks-fabric", color: "A844D4" }} snapshot /> |
| <MavenBadge repository='dev/matthiesen/matthiesen-lib-webhooks-neoforge' settings={{ name: "matthiesen-lib-webhooks-neoforge", color: "A844D4" }} snapshot /> |

### Step 3: Add Dependencies

The dependency you need depends on your project structure:

#### For Common/Shared Code

If you're writing multi-loader code in a common module:

```kotlin
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-common:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}
```

#### For Fabric-Specific Code

In your Fabric module:

```kotlin
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-fabric:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}
```

#### For NeoForge-Specific Code

In your NeoForge module:

```kotlin
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-neoforge:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}
```

#### Complete Multi-Loader Example

For a typical Architectury-style multi-loader setup:

```kotlin
// In common/build.gradle.kts
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-common:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}

// In fabric/build.gradle.kts
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-fabric:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}

// In neoforge/build.gradle.kts  
dependencies {
    modImplementation("dev.matthiesen:matthiesen-lib-webhooks-neoforge:${property("matthiesen_lib_webhooks_version")}") { 
        isTransitive = false 
    }
}
```

## Declaring as a Dependency

Make sure to declare Matthiesen Lib as a dependency in your mod metadata files so users know they need it.

### For Fabric

```json title="fabric.mod.json"
{
  "depends": {
    "matthiesen_lib_webhooks": ">=1.0.0"
  }
}
```

### For NeoForge

```toml title="neoforge.mods.toml"
[[dependencies.yourmodid]]
modId = "matthiesen_lib_webhooks"
type = "required"
versionRange = "[1.0.0,)"
ordering = "AFTER"
side = "SERVER"
```

## Verification

After adding the dependency, sync your Gradle project. You should now be able to import Matthiesen Lib classes:

```java
import dev.matthiesen.common.matthiesen_lib_webhooks.MatthiesenLibWebhooks;
```

## Troubleshooting

### Dependency Not Found

If Gradle can't find the dependency:
1. Verify the repository URL is correct
2. Check that you've included the correct group (`dev.matthiesen`)
3. Ensure the version exists on Maven by visiting [maven.matthiesen.dev](https://maven.matthiesen.dev/)
4. Try invalidating caches and restarting your IDE

### Version Conflicts

If you encounter version conflicts:
1. Ensure all modules use the same version of Matthiesen Lib Webhooks
2. Check that your Minecraft version is compatible (1.21.1)
3. Verify your loader versions meet the minimum requirements