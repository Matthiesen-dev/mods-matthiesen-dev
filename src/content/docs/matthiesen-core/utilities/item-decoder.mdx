---
title: "Item Decoder"
description: "Utility class for decoding items from strings."
banner:
  content: |
    These docs are a work in progress and may be incomplete or contain inaccuracies. (Or be for a newer unreleased version of the mod)
---

Utility class for decoding items from strings. If the string cannot be decoded, it will return the provided fallback item and log an error.

## `stringToItem`

Decodes an item from a string. The string should be in the format "namespace:path". If the string cannot be decoded, it will return the provided fallback item and log an error.

```java title="stringToItemExample.java"

import dev.matthiesen.matthiesen_core.common.utility.item.ItemDecoder;
import net.minecraft.world.item.Item;
import net.minecraft.world.item.Items;

public class Example {
    public static void main(String[] args) {
        String itemString = "minecraft:stone";
        Item fallbackItem = Items.AIR; // Fallback item if decoding fails

        Item decodedItem = ItemDecoder.stringToItem(itemString, fallbackItem);

        System.out.println("Decoded Item: " + decodedItem);
    }
}
```

## `stringToBlock`

Decodes a block from a string. The string should be in the format "namespace:path". If the string cannot be decoded, it will return the provided fallback block and log an error.

```java title="stringToBlockExample.java"

import dev.matthiesen.matthiesen_core.common.utility.item.ItemDecoder;
import net.minecraft.world.level.block.Block;
import net.minecraft.world.level.block.Blocks;

public class Example {
    public static void main(String[] args) {
        String blockString = "minecraft:stone";
        Block fallbackBlock = Blocks.AIR; // Fallback block if decoding fails

        Block decodedBlock = ItemDecoder.stringToBlock(blockString, fallbackBlock);

        System.out.println("Decoded Block: " + decodedBlock);
    }
}
```
